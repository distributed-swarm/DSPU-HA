[project]
name = "dspu-tests"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8.0",
  "httpx>=0.27",
  "psycopg[binary]>=3.1",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
